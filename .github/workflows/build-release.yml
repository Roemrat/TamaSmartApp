name: Build and Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v1.0.0)'
        required: true
        default: 'v1.0.0'
      create_release:
        description: 'Create GitHub Release'
        required: false
        type: boolean
        default: true

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
        
    - name: Find project file
      id: find_project
      run: |
        if (Test-Path "TamaSmartApp.csproj") {
          echo "project_path=TamaSmartApp.csproj" >> $env:GITHUB_OUTPUT
        } elseif (Test-Path "TamaSmartApp/TamaSmartApp.csproj") {
          echo "project_path=TamaSmartApp/TamaSmartApp.csproj" >> $env:GITHUB_OUTPUT
        } else {
          echo "Project file not found!"
          Get-ChildItem -Recurse -Filter "*.csproj" | Select-Object -First 1 FullName
          exit 1
        }
      shell: powershell
      
    - name: Restore dependencies
      run: dotnet restore ${{ steps.find_project.outputs.project_path }}
      
    - name: Build Release
      run: dotnet build ${{ steps.find_project.outputs.project_path }} -c Release
      
    - name: Publish Release
      run: dotnet publish ${{ steps.find_project.outputs.project_path }} -c Release -r win-x86 --self-contained false
      
    - name: Get Version
      id: get_version
      run: |
        $version = "${{ github.event.inputs.version }}"
        if ([string]::IsNullOrWhiteSpace($version)) {
          $version = "v${{ github.run_number }}"
        }
        echo "version=$version" >> $env:GITHUB_OUTPUT
        echo "VERSION=$version" >> $env:GITHUB_ENV
      shell: powershell
      
    - name: Find publish directory
      id: find_publish
      run: |
        $publishPath = if (Test-Path "bin\Release\net48\win-x86\publish") {
          "bin\Release\net48\win-x86\publish"
        } elseif (Test-Path "TamaSmartApp\bin\Release\net48\win-x86\publish") {
          "TamaSmartApp\bin\Release\net48\win-x86\publish"
        } else {
          Get-ChildItem -Recurse -Filter "TamaSmartApp.exe" -Directory | Where-Object { $_.FullName -like "*publish*" } | Select-Object -First 1 -ExpandProperty FullName
        }
        echo "publish_path=$publishPath" >> $env:GITHUB_OUTPUT
      shell: powershell
      
    - name: Remove unnecessary files
      run: |
        $publishPath = "${{ steps.find_publish.outputs.publish_path }}"
        $configFile = Join-Path $publishPath "TamaSmartApp.exe.config"
        $pdbFile = Join-Path $publishPath "TamaSmartApp.pdb"
        if (Test-Path $configFile) {
          Remove-Item $configFile -Force
          echo "Removed TamaSmartApp.exe.config"
        }
        if (Test-Path $pdbFile) {
          Remove-Item $pdbFile -Force
          echo "Removed TamaSmartApp.pdb"
        }
      shell: powershell
      
    - name: Create Release Directory
      run: |
        New-Item -ItemType Directory -Force -Path "release" | Out-Null
        Copy-Item -Path "${{ steps.find_publish.outputs.publish_path }}\*" -Destination "release\" -Recurse -Force
      shell: powershell
      
    - name: Create ZIP Archive
      run: |
        $version = "${{ steps.get_version.outputs.version }}"
        $zipName = "TamaSmartApp-$version-win-x86.zip"
        Compress-Archive -Path "release\*" -DestinationPath $zipName -Force
        echo "ZIP_FILE=$zipName" >> $env:GITHUB_ENV
      shell: powershell
      
    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: TamaSmartApp-win-x86
        path: TamaSmartApp-*.zip
        retention-days: 30
        
    - name: Create Release
      if: github.event.inputs.create_release == 'true'
      uses: softprops/action-gh-release@v1
      with:
        files: TamaSmartApp-*.zip
        tag_name: ${{ steps.get_version.outputs.version }}
        name: Release ${{ steps.get_version.outputs.version }}
        draft: false
        prerelease: false
        generate_release_notes: true
        body: |
          ## Tama Smart App Release ${{ steps.get_version.outputs.version }}
          
          ### Installation
          1. Download `TamaSmartApp-${{ steps.get_version.outputs.version }}-win-x86.zip`
          2. Extract the ZIP file
          3. Run `TamaSmartApp.exe`
          
          ### Requirements
          - Windows (32-bit)
          - .NET Framework 4.8
          - CH347 USB-to-SPI Bridge
          
          ### Features
          - Read/Write SPI Flash Memory
          - Chip ID Detection
          - Theme Detection for Tamagotchi SMA Card
          - Reset Tama Memory
          - Full Chip Erase
          
          ### Build Information
          - **Build Number**: ${{ github.run_number }}
          - **Commit**: ${{ github.sha }}
          - **Branch**: ${{ github.ref_name }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
